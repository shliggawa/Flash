# Flash Module Catalog

This repository hosts the early Flash module catalog and downloadable module packages.

Flash reads `catalog.json`, downloads module zip files from `downloads/`, verifies SHA256, and installs them into the local Flash modules folder.
